import 'package:flutter/material.dart';

class DashbordeForAdmin extends StatelessWidget {
  const DashbordeForAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
